import styled from 'styled-components';

const HeaderWrapper = styled.div`
  background: #f16550;
  color: #fff;
  padding: 20px;
  font-size: 20px;
  font-weight: 700;
`;

export default HeaderWrapper;
